{
  "buildCommand": "echo skip",
  "outputDirectory": ".",
  "functions": {
    "api/analyze.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/analyze", "dest": "/api/analyze" },
    { "handle": "filesystem" }
  ]
}
